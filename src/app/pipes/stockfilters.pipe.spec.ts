import { StockfiltersPipe } from './stockfilters.pipe';

describe('StockfiltersPipe', () => {
  it('create an instance', () => {
    const pipe = new StockfiltersPipe();
    expect(pipe).toBeTruthy();
  });
});
