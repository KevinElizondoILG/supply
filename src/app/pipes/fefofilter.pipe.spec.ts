import { FefofilterPipe } from './fefofilter.pipe';

describe('FefofilterPipe', () => {
  it('create an instance', () => {
    const pipe = new FefofilterPipe();
    expect(pipe).toBeTruthy();
  });
});
