import { TotalstockPipe } from './totalstock.pipe';

describe('TotalstockPipe', () => {
  it('create an instance', () => {
    const pipe = new TotalstockPipe();
    expect(pipe).toBeTruthy();
  });
});
